deployment:
  replicas: 1

ingressClass:
  enabled: true
  isDefaultClass: true

service:
  enabled: true
  type: LoadBalancer

ports:
  web:
    redirectTo:
      port: websecure
  websecure:
    tls:
      enabled: true

logs:
  general:
    level: INFO
